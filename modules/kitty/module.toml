description = "OpenGL-based terminal emulator with user configuration"
[packages]
present = [
  "kitty", # A modern, hackable, featureful, OpenGL-based terminal emulator
]

[dotfiles]
"~/.config/kitty" = { source = "home/.config/kitty", mode = "symlink-each" }
