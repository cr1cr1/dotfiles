description = "C++ system resource monitor with its configuration"
[packages]
present = [
  "btop", # A monitor of system resources, bpytop ported to C++
]

[dotfiles]
"~/.config/btop" = { source = "home/.config/btop", mode = "symlink-each" }
