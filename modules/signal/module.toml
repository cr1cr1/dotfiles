description = "Signal private messenger desktop client with launch flags"
[packages]
present = [
  "signal-desktop", # Signal Private Messenger for Linux
]

[dotfiles]
"~/.config/signal-desktop-flags.conf" = { source = "home/.config/signal-desktop-flags.conf", mode = "symlink" }
