description = "Terminal UI for searching Arch Linux repositories and the AUR"
[packages]
present = [
  "pacseek",
]

[dotfiles]
"~/.config/pacseek" = { source = "home/.config/pacseek", mode = "symlink-each" }
