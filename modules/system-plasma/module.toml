description = "System-wide KDE Plasma login configuration via /etc/plasmalogin.conf"
scope = "system"

[dotfiles]
"/etc/plasmalogin.conf" = { source = "home/etc/plasmalogin.conf", mode = "copy" }

[hooks]
post = ["sudo systemctl enable --now plasmalogin"]
