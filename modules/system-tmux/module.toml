description = "System-wide tmux terminal multiplexer with a shared /etc/tmux.conf"
scope = "system"

[packages]
present = [
  "tmux", # Terminal multiplexer
]

[dotfiles]
"/etc/tmux.conf" = { source = "home/etc/tmux.conf", mode = "copy" }
