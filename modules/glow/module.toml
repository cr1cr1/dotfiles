description = "Command-line markdown renderer with its configuration"
[packages]
present = [
  "glow", # Command-line markdown renderer
]

[dotfiles]
"~/.config/glow" = { source = "home/.config/glow", mode = "symlink-each" }
