description = "System locale configuration (en_US.UTF-8)"
scope = "system"

[dotfiles]
"/etc/locale.conf" = { source = "etc/locale.conf", mode = "copy" }
"/etc/default/locale" = { source = "etc/default/locale", mode = "copy" }

[hooks]
post = ["sudo locale-gen"]
