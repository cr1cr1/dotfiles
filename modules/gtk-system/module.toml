description = "Mirrors the GTK 2 RC file to /root for consistent theming"
# Mirrors the user's ~/.gtkrc-2.0 to /root (from
# apps/gtk/config/.gtkrc-2.0 in pimp-my-cachyos).
scope = "system"

[dotfiles]
"/root/.gtkrc-2.0" = { source = "home/.gtkrc-2.0", mode = "copy" }
